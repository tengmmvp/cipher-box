name: CI

on:
  push:
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
        python-version: ["3.10", "3.12"]
    runs-on: ${{ matrix.os }}
    env:
      QT_QPA_PLATFORM: offscreen
    steps:
      - uses: actions/checkout@v5
      - name: Install Qt system libraries (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libegl1 libgl1 libdbus-1-3 libxkbcommon0 libfontconfig1 \
            libxcb-cursor0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 \
            libxcb-randr0 libxcb-render-util0 libxcb-shape0 libxcb-sync1 \
            libxcb-xfixes0 libxcb-xkb1 libxkbcommon-x11-0 libxcb-xinerama0
      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          python-version: ${{ matrix.python-version }}
          enable-cache: true
      - name: Install dependencies
        run: uv sync --locked --extra dev
      - run: uv run ruff check src tests
      - run: uv run mypy src
      - name: Tests with coverage
        run: uv run pytest --cov=src --cov-report=term-missing --cov-report=xml
      # 分层覆盖率门槛：非 UI 85% / UI 30%，复用同一 .coverage
      - name: Non-UI coverage gate
        run: uv run coverage report --omit="*/src/ui/*" --fail-under=85
      - name: UI coverage gate
        run: uv run coverage report --include="*/src/ui/*" --fail-under=30
      # pip-audit 经 --with 临时运行，不入 uv.lock
      - name: Dependency vulnerability scan
        run: uv run --with pip-audit pip-audit
